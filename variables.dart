/**
 * Dart follows the same rules for declaring variables like we have in C and JS.
 * Also, it throws error if you assign different data type value to different data type variable like when you implement
 * bool first = 'abc
 */
main(List<String> args) {
  bool IamBool = true;
}
